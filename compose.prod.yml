services:
  app:
    image: ghcr.io/jens0102/autista-bewerbertest:latest
    ports:
      - "8080:80"
    volumes:
      - autista-storage:/var/www/html/storage
    restart: unless-stopped

volumes:
  autista-storage:
