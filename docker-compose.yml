services:
  app:
    build: .
    image: autista-bewerbertest:local
    ports:
      - "8080:80"
    volumes:
      - autista-storage:/var/www/html/storage
    restart: unless-stopped

volumes:
  autista-storage:
